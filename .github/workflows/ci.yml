name: CI

on:
  pull_request:
    paths-ignore:
      - "**.md"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/dependabot.yml"
      - "vrsn.toml"
  push:
    branches: [main]
    paths-ignore:
      - "**.md"
      - ".github/ISSUE_TEMPLATE/**"
      - ".github/dependabot.yml"
      - "vrsn.toml"
  workflow_call:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  # Detect which areas a PR touches so the heavy native jobs can skip when they're
  # irrelevant. On push/main (and workflow_call) the gates below force every job to run
  # regardless — main is always fully tested; path-skipping only trims PR runs.
  changes:
    runs-on: ubuntu-latest
    outputs:
      zig: ${{ steps.filter.outputs.zig }}
      android: ${{ steps.filter.outputs.android }}
      components: ${{ steps.filter.outputs.components }}
    steps:
      - uses: actions/checkout@v7
      - uses: dorny/paths-filter@v4
        id: filter
        with:
          filters: |
            shared: &shared
              - 'MODULE.bazel'
              - 'MODULE.bazel.lock'
              - '.bazelversion'
              - '.bazelrc'
              - 'patches/**'
              - 'BUILD.bazel'
              - '.github/workflows/ci.yml'
              - '.github/actions/**'
            zig:
              - *shared
              - 'atolla/native/zig/**'
            android:
              - *shared
              - 'atolla/native/**'
            components:
              - *shared
              - 'atolla/**'
              - 'package.json'
              - 'bun.lock'
              - 'tsconfig*.json'
  static-checks:
    runs-on: ubuntu-latest
    # cold path (projectsync cache miss + cold remote cache) measured ~25 min,
    # plus the projectsync-outputs save on top
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
      - name: cache bun dependencies
        uses: actions/cache@v6
        with:
          path: ~/.bun/install/cache
          key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
          restore-keys: |
            bun-${{ runner.os }}-
      - run: bun install --frozen-lockfile
      # projectsync's generated res.ts is a typed accessor keyed by resource
      # name/path, not file bytes — so key the cache on the sorted file LIST, not
      # hashFiles (which hashes contents). A reformat or icon redesign that keeps
      # the same filenames still hits; adds/removes/renames miss (as they should).
      - name: hash res file list
        id: reslist
        run: echo "hash=$(cd atolla && find res -type f | LC_ALL=C sort | sha256sum | cut -d' ' -f1)" >> "$GITHUB_OUTPUT"
      # tsc only needs projectsync's OUTPUTS: the valdi sources/types, the
      # generated res.ts, and the linux-rewritten tsconfig. The heavy outputs
      # (generated_ts + valdi module sources) are built by the bazel Valdi
      # toolchain pinned in MODULE.bazel (VALDI_TAG), NOT the npm @snap/valdi CLI
      # — which only orchestrates bazel and rewrites atolla/tsconfig.json. So the
      # key covers the bazel dep-set (MODULE.bazel etc.) + BUILD.bazel + the res
      # file list, and stays hit across CLI-only bumps — on a hit this job runs no
      # bazel at all (the 22-minute projectsync build disappears).
      - name: restore projectsync outputs
        id: projectsync-cache
        uses: actions/cache/restore@v6
        with:
          path: |
            bazel-atolla/external/valdi~/modules
            bazel-atolla/external/valdi~/src/valdi_modules
            bazel-out/k8-fastbuild/bin/atolla/.valdi_build/projectsync/generated_ts
            bazel-out/k8-fastbuild/bin/external/valdi~/src/valdi_modules/src/valdi/valdi_test/.valdi_build/projectsync/generated_ts
            atolla/tsconfig.json
          key: projectsync-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock', '.bazelversion', 'patches/**', 'atolla/BUILD.bazel') }}-res${{ steps.reslist.outputs.hash }}
      # only the cold path runs bazel (projectsync builds Skia/Hermes/etc), so only
      # it needs the extra room; a cache hit does no heavy build
      - name: free disk space
        if: steps.projectsync-cache.outputs.cache-hit != 'true'
        uses: ./.github/actions/free-disk-space
      - name: Install libtinfo5
        if: steps.projectsync-cache.outputs.cache-hit != 'true'
        uses: ./.github/actions/install-libtinfo5
      - name: Configure bazel caching
        if: steps.projectsync-cache.outputs.cache-hit != 'true'
        uses: ./.github/actions/configure-bazel-caching
        with:
          buildbuddy-api-key: ${{ secrets.BUILDBUDDY_API_KEY }}
      # ONE shared repository cache across all jobs (see android-tests for the full
      # rationale). It's the content-addressed download store (llvm, NDK, JDK,
      # skia/hermes sources) — identical across jobs — so it's keyed on the dep-set
      # alone and only android-tests (a superset fetch) saves it. Restore-only here.
      - name: restore bazel repository cache
        if: steps.projectsync-cache.outputs.cache-hit != 'true'
        id: restore-bazel-repo
        uses: actions/cache/restore@v6
        with:
          path: ~/.cache/bazel-repo
          key: bazel-repo-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock', '.bazelversion', 'patches/**') }}
          restore-keys: |
            bazel-repo-${{ runner.os }}-
      # Join the backgrounded disk cleanup before the first heavy bazel build.
      - name: wait for disk cleanup
        if: steps.projectsync-cache.outputs.cache-hit != 'true'
        uses: ./.github/actions/wait-for-disk-cleanup
      - run: bunx valdi projectsync --target //atolla:atolla_projectsync
        id: projectsync
        if: steps.projectsync-cache.outputs.cache-hit != 'true'
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0
      - run: bun run check:static
      - name: fail on formatting or lint drift
        run: git diff --exit-code -- . ':!atolla/tsconfig.json'
      # outcome guard: a projectsync that died midway must never get cached
      # under the canonical key (it would poison every later run); still banks
      # the cache when only check:static or the drift check fails
      - name: save projectsync outputs
        if: ${{ !cancelled() && steps.projectsync-cache.outputs.cache-hit != 'true' && steps.projectsync.outcome == 'success' }}
        uses: actions/cache/save@v6
        with:
          path: |
            bazel-atolla/external/valdi~/modules
            bazel-atolla/external/valdi~/src/valdi_modules
            bazel-out/k8-fastbuild/bin/atolla/.valdi_build/projectsync/generated_ts
            bazel-out/k8-fastbuild/bin/external/valdi~/src/valdi_modules/src/valdi/valdi_test/.valdi_build/projectsync/generated_ts
            atolla/tsconfig.json
          key: projectsync-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock', '.bazelversion', 'patches/**', 'atolla/BUILD.bazel') }}-res${{ steps.reslist.outputs.hash }}

  zig-tests:
    needs: changes
    if: ${{ needs.changes.outputs.zig == 'true' || github.event_name != 'pull_request' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
      - name: free disk space
        uses: ./.github/actions/free-disk-space
      - uses: mlugg/setup-zig@v2
        with:
          version: 0.16.0
      - run: bun run test:zig
      - name: configure bazel caching
        uses: ./.github/actions/configure-bazel-caching
        with:
          buildbuddy-api-key: ${{ secrets.BUILDBUDDY_API_KEY }}
          remote-download-minimal: "true"
      # Restore-only; the shared repo cache is saved by android-tests (see there).
      - name: Restore bazel repository cache
        id: restore-bazel-repo
        uses: actions/cache/restore@v6
        with:
          path: ~/.cache/bazel-repo
          key: bazel-repo-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock', '.bazelversion', 'patches/**') }}
          restore-keys: |
            bazel-repo-${{ runner.os }}-
      - name: wait for disk cleanup
        uses: ./.github/actions/wait-for-disk-cleanup
      - run: bun run check:zig

  component-tests:
    needs: changes
    if: ${{ needs.changes.outputs.components == 'true' || github.event_name != 'pull_request' }}
    runs-on: ubuntu-latest
    timeout-minutes: 120
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
      - name: free disk space
        uses: ./.github/actions/free-disk-space
      - name: Install libtinfo5
        uses: ./.github/actions/install-libtinfo5
      # Pin the output base so it can be cached: restoring it gives a warm build
      # (extracted external repos + bazel-out) with no re-extraction or recompile.
      # It's SAVED only on the default branch (below), so MR runs read main's warm
      # base but never write multi-GB entries that would churn the shared 10 GB pool.
      # No --remote_download_minimal below: this warm base already holds the outputs.
      - name: pin bazel output base
        run: echo "startup --output_base=$HOME/.cache/bazel-ob" >> ~/.bazelrc
      - name: configure bazel caching
        uses: ./.github/actions/configure-bazel-caching
        with:
          buildbuddy-api-key: ${{ secrets.BUILDBUDDY_API_KEY }}
      # Restore the warm output base (extracted repos + bazel-out) — a hit means no
      # re-extraction and no recompile. Keyed on the dependency set (MODULE/patches), NOT
      # the commit SHA: a per-SHA key banked a fresh multi-GB base on every main push, and a
      # handful of those overflow the shared 10 GB Actions pool and evict each other (and the
      # repo caches) — so the base was gone by the next run and component-tests fell back to a
      # full cold build. One base per dependency set is all we need: the expensive artifacts
      # (Hermes/Skia/Valdi core) only change when the dependency set does, so within a set
      # bazel just rebuilds the cheap app/test sources incrementally off the restored base.
      # SAVED only on the default branch (below), so MR runs read main's base but never write.
      - name: restore bazel output base
        id: restore-bazel-ob
        uses: actions/cache/restore@v6
        with:
          path: ~/.cache/bazel-ob
          key: bazel-ob-${{ github.job }}-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock', '.bazelversion', 'patches/**') }}
          restore-keys: |
            bazel-ob-${{ github.job }}-${{ runner.os }}-
      # cold path only — a restored output base already contains every extracted repo
      - name: restore bazel repository cache
        if: steps.restore-bazel-ob.outputs.cache-matched-key == ''
        id: restore-bazel-repo
        uses: actions/cache/restore@v6
        with:
          path: ~/.cache/bazel-repo
          key: bazel-repo-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock', '.bazelversion', 'patches/**') }}
          restore-keys: |
            bazel-repo-${{ runner.os }}-
      - name: wait for disk cleanup
        uses: ./.github/actions/wait-for-disk-cleanup
      - run: bun run test:components
        timeout-minutes: 120
      - name: shut down bazel server
        if: always()
        run: bazel shutdown && du -sh ~/.cache/bazel-ob || true
      # Save the multi-GB caches ONLY on the default branch so MR runs never write to (and
      # churn) the shared Actions pool. MR runs still get per-push reuse via BuildBuddy. With
      # the dep-set key above, main banks exactly one base per dependency set (the cache-hit
      # guard skips the save once it exists) instead of a fresh multi-GB entry per commit.
      - name: save bazel output base
        if: always() && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && steps.restore-bazel-ob.outputs.cache-hit != 'true'
        uses: actions/cache/save@v6
        with:
          path: ~/.cache/bazel-ob
          key: bazel-ob-${{ github.job }}-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock', '.bazelversion', 'patches/**') }}

  android-tests:
    needs: changes
    if: ${{ needs.changes.outputs.android == 'true' || github.event_name != 'pull_request' }}
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v7
      - uses: oven-sh/setup-bun@v2
      - name: free disk space
        uses: ./.github/actions/free-disk-space
      - name: install libtinfo5
        uses: ./.github/actions/install-libtinfo5
      - name: configure bazel caching
        uses: ./.github/actions/configure-bazel-caching
        with:
          buildbuddy-api-key: ${{ secrets.BUILDBUDDY_API_KEY }}
          remote-download-minimal: "true"
      # ONE shared repository cache for every job. ~/.cache/bazel-repo is bazel's
      # --repository_cache (.bazelrc): a content-addressed store of downloaded
      # tarballs (llvm, NDK, JDK, skia/hermes sources). Each artifact is byte-
      # identical no matter which job/target fetched it and the store is additive,
      # so there's no reason to partition by job — doing that minted a ~3.7 GiB copy
      # per job (~15 GiB across the jobs) that thrashed the 10 GiB Actions pool and
      # evicted the projectsync-outputs and bazel-ob caches every run. Keyed on the
      # dep-set alone; android-tests is the sole saver (below) because its download
      # set is a superset (host toolchain + the Android NDK/SDK). Every other job
      # restores read-only; a partial hit self-heals as bazel fetches only what's missing.
      - name: restore bazel repository cache
        id: restore-bazel-repo
        uses: actions/cache/restore@v6
        with:
          path: ~/.cache/bazel-repo
          key: bazel-repo-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock', '.bazelversion', 'patches/**') }}
          restore-keys: |
            bazel-repo-${{ runner.os }}-
      - name: wait for disk cleanup
        uses: ./.github/actions/wait-for-disk-cleanup
      - run: bun run test:android
        timeout-minutes: 30
      # Sole saver of the shared repo cache (superset: host toolchain + NDK), on
      # main only so PR runs restore read-only and never churn the 10 GiB pool.
      - name: save bazel repository cache
        if: always() && github.ref == format('refs/heads/{0}', github.event.repository.default_branch) && steps.restore-bazel-repo.outputs.cache-hit != 'true'
        uses: actions/cache/save@v6
        with:
          path: ~/.cache/bazel-repo
          key: bazel-repo-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock', '.bazelversion', 'patches/**') }}

  # macos_unit_test targets need Xcode/Apple SDKs — the only tests that can't run on linux
  # ios-tests:
  #   runs-on: macos-latest
  #   timeout-minutes: 60
  #   steps:
  #     - uses: actions/checkout@v7
  #     - uses: oven-sh/setup-bun@v2
  #     - name: cache bun dependencies
  #       uses: actions/cache@v6
  #       with:
  #         path: ~/.bun/install/cache
  #         key: bun-${{ runner.os }}-${{ hashFiles('bun.lock') }}
  #         restore-keys: |
  #           bun-${{ runner.os }}-
  #     - run: bun install --frozen-lockfile
  #     - name: configure bazel caching
  #       env:
  #         BUILDBUDDY_API_KEY: ${{ secrets.BUILDBUDDY_API_KEY }}
  #       run: |
  #         echo "build --disk_cache=" >> ~/.bazelrc
  #         if [ -n "$BUILDBUDDY_API_KEY" ]; then
  #           echo "build --config=remote-cache" >> ~/.bazelrc
  #           echo "build --remote_header=x-buildbuddy-api-key=$BUILDBUDDY_API_KEY" >> ~/.bazelrc
  #         fi
  #     - name: cache bazel repository cache
  #       uses: actions/cache@v6
  #       with:
  #         path: ~/.cache/bazel-repo
  #         key: bazel-repo-${{ runner.os }}-${{ hashFiles('MODULE.bazel', 'MODULE.bazel.lock', '.bazelversion', 'patches/**') }}
  #         restore-keys: |
  #           bazel-repo-${{ runner.os }}-
  #     - run: bun run test:ios
