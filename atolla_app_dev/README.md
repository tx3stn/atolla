# `atolla_app_dev`

Valdi module to build the dev version of the ios and android apps.

The dev version is a separate build target, with a different name and icon so
you can install it next to an official install.

It includes a `DEVELOPER` section in settings to trigger some functionality that
would be otherwise annoying to test manually.
